# EntoField 0.9.2 - Logo Loading Hotfix

- Serve the Field Ink logo assets directly instead of routing them through the
  framework image optimizer.
- Fix broken logo images on the Cloudflare Workers deployment.
- Refresh the offline cache so installed copies receive the corrected header.
